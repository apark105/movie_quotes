import React from 'react';

export default props => (
    <div>
        <h1 className="center">About Us</h1>
        <p>By default for buttons, types are ‘submit’. By default form submit the data via get, sends the data through the get method.Everything after the question marks, query strings are made up of key/value pairs. </p>
        <p>By default for buttons, types are ‘submit’. By default form submit the data via get, sends the data through the get method.Everything after the question marks, query strings are made up of key/value pairs. </p>
        <p>By default for buttons, types are ‘submit’. By default form submit the data via get, sends the data through the get method.Everything after the question marks, query strings are made up of key/value pairs. </p>
    </div>
)